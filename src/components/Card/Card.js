import React from "react";
import "./Card.css";

function Card() {
  return (
    <div
      className="card"
      style={{
        // backgroundImage: props.image ? `url(${props.image})` : "none",
      }}
    >
    </div>
  );
}

export default Card;
